So I need a layer to be added that allows the management and orchetration of multiple agents.
I need a sub-agent registry which contains a list of agent configs:
* Add the ability to configure the system prompt via the agent.toml
* Create a directory which contains multiple agent.tomls each configuring a unique agent
* The agent registry collects the *.tomls from the agent directory and loads them into an agent config registry
* Each agent in the agent repo is indexed by agent name, the agent is dynamically configured and returned from the config when it is requested from the agent repo
* Should the agent repo follow the Agent Card specification?
* The agent toml should specify the name of the agent and a condensed description of what it is useful for so that an orchestration layer can select the correct sub agent for a task

Notes on tech debt to clean up
1) Currently there seems to be a domain leakage from the evals into the agent code.
e.g. in agent/__main__.py there is an if args_chat and then an if to select between a chat entry point and an agent with a task entry point.
These should probably be the same code path.
MockToolRegistry has bespoke code in agent/mcp/premissions Better if MockToolRegistry implements the same interface as the real MCP registry and is injected when the agent is composed in the eval harness.
Refactor so the task and the chat are running through a selected config for an agent
Also the chat path doesn't seem to enforce any permissions?

Should the agent orchestrator itself be an agent configuration?

How should this code be deployed as a library for multiple agent implementations?

Memory system, is it additional information included in the system prompt, or is it accessed via a tool call?
The obsidian system itself could act as long term curated memory?

what if I want the system to run multiple experiments? How do I design the evaluator?
Easy to implement as a tool call called by the agent to evaluate its result once complete :+1: